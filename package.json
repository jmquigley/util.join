{
  "name": "util.join",
  "version": "0.0.10",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Joins and normalizes a file path to a unix path.",
  "private": false,
  "debug": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.join"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.join/issues"
  },
  "engines": {
    "node": ">=7.5"
  },
  "dependencies": {
    "util.home": "0.0.15",
    "util.pkgwrap": "0.0.43"
  },
  "devDependencies": {
    "@types/node": "^7.0.21",
    "ava": "^0.19.1",
    "coveralls": "^2.13.1",
    "nyc": "^10.3.2",
    "tslint": "^5.2.0",
    "typescript": "^2.3.2"
  },
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "all": "npm run lint && npm run clean && npm run build && npm test && npm run report",
    "build": "pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "lint": "pkgwrap --lint",
    "postinstall": "pkgwrap --postinstall",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  }
}
